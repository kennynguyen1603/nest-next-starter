NEXT_PUBLIC_API_URL=http://localhost:8080

# Set to true to enable the Notifications section in the admin panel.
# Must match NOTIFICATIONS_ENABLED=true in apps/api/.env.
NEXT_PUBLIC_NOTIFICATIONS_ENABLED=false
