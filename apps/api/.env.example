# -------------------------------------------------------
# App
# -------------------------------------------------------
NODE_ENV=development
APP_NAME=nest-next-starter
APP_PORT=8080
API_PREFIX=api
FRONTEND_DOMAIN=http://localhost:3000
BACKEND_DOMAIN=http://localhost:8080
APP_FALLBACK_LANGUAGE=en
APP_HEADER_LANGUAGE=x-custom-lang

# -------------------------------------------------------
# Database (MongoDB)
# -------------------------------------------------------
DATABASE_TYPE=mongodb
DATABASE_URL=mongodb://localhost:27017/nest_starter
# Or use individual fields instead of DATABASE_URL:
# DATABASE_HOST=localhost
# DATABASE_PORT=27017
# DATABASE_USERNAME=
# DATABASE_PASSWORD=
# DATABASE_NAME=nest_starter
DATABASE_SYNCHRONIZE=false
DATABASE_MAX_CONNECTIONS=100
DATABASE_SSL_ENABLED=false
DATABASE_REJECT_UNAUTHORIZED=false

# -------------------------------------------------------
# Auth (JWT)
# -------------------------------------------------------
AUTH_JWT_SECRET=change_me_jwt_secret
AUTH_JWT_TOKEN_EXPIRES_IN=15m
AUTH_REFRESH_SECRET=change_me_refresh_secret
AUTH_REFRESH_TOKEN_EXPIRES_IN=7d
AUTH_FORGOT_SECRET=change_me_forgot_secret
AUTH_FORGOT_TOKEN_EXPIRES_IN=30m
AUTH_CONFIRM_EMAIL_SECRET=change_me_confirm_secret
AUTH_CONFIRM_EMAIL_TOKEN_EXPIRES_IN=1d

# -------------------------------------------------------
# Mail
# -------------------------------------------------------
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USER=
MAIL_PASSWORD=
MAIL_DEFAULT_EMAIL=noreply@example.com
MAIL_DEFAULT_NAME=App
MAIL_IGNORE_TLS=false
MAIL_SECURE=false
MAIL_REQUIRE_TLS=true

# -------------------------------------------------------
# File storage (S3)
# -------------------------------------------------------
# Options: local | s3 | s3-presigned | cloudinary
FILE_DRIVER=s3
ACCESS_KEY_ID=your_aws_access_key_id
SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_DEFAULT_S3_BUCKET=your-bucket-name
AWS_S3_REGION=ap-southeast-1

# Cloudinary (only needed when FILE_DRIVER=cloudinary)
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=

# -------------------------------------------------------
# Google OAuth (optional)
# -------------------------------------------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
